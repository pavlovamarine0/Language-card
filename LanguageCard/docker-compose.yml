
volumes:
  pgdata:


services:
  db:
    image: postgres:latest
    container_name: Language_Card_db
    environment: 
      POSTGRES_PASSWORD: postgrespw 
    ports: 
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql